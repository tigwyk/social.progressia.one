apiVersion: v1
kind: Service
metadata:
  name: {{ include "ogcard.fullname" . }}
  labels:
    {{- include "ogcard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "ogcard.selectorLabels" . | nindent 4 }}